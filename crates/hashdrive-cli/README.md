# hdrive

CLI and daemon for Hashdrive end-user file sync.

The `hdrive` binary supervises a local hashtree daemon, indexes user-chosen
sync roots, and exposes status/share commands the native app shells call.

Run `hdrive --help` for the current command set.

## Repository

`htree://self/hashdrive`
