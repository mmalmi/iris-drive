# hashdrive-core

Shared Hashdrive runtime support.

Owns the configuration models, sync state, htree-daemon client wrapper, and
share model used by the `hdrive` CLI/daemon and native app shells.

The public API is still evolving.

## Repository

`htree://self/hashdrive`
