# iris-drive-core

Shared Iris Drive runtime support.

Owns the configuration models, sync state, htree-daemon client wrapper, and
share model used by the `idrive` CLI/daemon and native app shells.

The public API is still evolving.

## Repository

`htree://self/iris-drive`
